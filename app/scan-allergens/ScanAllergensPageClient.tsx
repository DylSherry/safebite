"use client";
import ScanAllergens from "../components/ScanAllergens";

export default function ScanAllergensPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8 bg-emerald-950">
      <ScanAllergens />
    </main>
  );
}
